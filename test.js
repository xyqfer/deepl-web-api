const translate = require('./index');

(async () => {
    console.log(await translate('Definition and Usage. The JSON.stringify() method converts JavaScript objects into strings. When sending data to a web server the data has to be a string.'))
})();